# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
*.cert
*.crt
mosquitto/certs/
config.json
secrets.h
secrets/

# Node
node_modules/
dist/

# Test seed files with hardcoded secrets (not for commit)
backend/seed_test.*
backend/prisma/seed_test.*

# Python
__pycache__/
*.pyc
.venv/
venv/
native-venv/
*.log

# Simulator artifacts
*.last_payload.json

# Runtime uploads (photos attached to events / face enrollments)
backend/uploads/
simulator/offline_buffer.db
simulator/*.json.bak

# Phone deployment bundle (contains live device credentials)
phone-package/

# Simulator config variants (example is tracked)
simulator/config.*.json
!simulator/config.example.json
!simulator/config.ngrok.example.json

# Face-service runtime store (persisted enrollments) — regenerated on enroll
face-service/enrollments.json

# Scanner-bridge runtime state (device counter)
scanner-bridge/state.json

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
